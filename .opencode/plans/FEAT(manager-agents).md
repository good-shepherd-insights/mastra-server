# FEAT(manager-agents)

## Request

Create three manager agents with Slack channel adapters. No tools, no memory — agents only.

## Status

`Approved — implementing.`

---

## Directory Map

```text
src/mastra/agents/research-manager.ts   — CREATE
src/mastra/agents/operations-manager.ts — CREATE
src/mastra/agents/qa-manager.ts         — CREATE
src/mastra/index.ts                     — MODIFY (register 3 agents)
```

---

## Slack Adapter API (verified from installed types)

```typescript
createSlackAdapter({
  botToken: process.env.RESEARCH_MANAGER_SLACK_BOT_TOKEN,
  signingSecret: process.env.RESEARCH_MANAGER_SLACK_SIGNING_SECRET,
})
```

Each agent gets its own token/signingSecret pair → separate Slack bot per agent.

---

## Agent Skeleton (Mastra docs pattern)

```typescript
import { Agent } from '@mastra/core/agent'
import { createSlackAdapter } from '@chat-adapter/slack'

export const agent = new Agent({
  id: 'agent-id',
  name: 'Agent Name',
  instructions: '...',
  model: 'auth-gateway/featherless/zai-org/GLM-5.1',
  channels: {
    adapters: {
      slack: createSlackAdapter({
        botToken: process.env.AGENT_SLACK_BOT_TOKEN,
        signingSecret: process.env.AGENT_SLACK_SIGNING_SECRET,
      }),
    },
  },
})
```

---

## Webhook Endpoints (auto-generated by Mastra)

| Agent | Webhook path |
|---|---|
| Research Manager | `/api/agents/research-manager/channels/slack/webhook` |
| Operations Manager | `/api/agents/operations-manager/channels/slack/webhook` |
| QA Manager | `/api/agents/qa-manager/channels/slack/webhook` |

---

## Env Vars Required

```bash
RESEARCH_MANAGER_SLACK_BOT_TOKEN=xoxb-...
RESEARCH_MANAGER_SLACK_SIGNING_SECRET=...
OPS_MANAGER_SLACK_BOT_TOKEN=xoxb-...
OPS_MANAGER_SLACK_SIGNING_SECRET=...
QA_MANAGER_SLACK_BOT_TOKEN=xoxb-...
QA_MANAGER_SLACK_SIGNING_SECRET=...
```
